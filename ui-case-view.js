function renderCaseView(caseId, rawPageIndex) {
  const data=getCase(caseId); const pageIndex=clamp(rawPageIndex,0,data.pages.length-1); const page=data.pages[pageIndex];
  state.lastCaseId=data.id; state.lastPageIndex=pageIndex; state.caseProgress[data.id]=Math.max(state.caseProgress[data.id]||0,pageIndex); state.visitedPages[data.id]=[...new Set([...(state.visitedPages[data.id]||[]),page.id])]; saveState();
  const completion=getPageCompletion(data,page);
  return `<main class="main case-main"><header class="case-page-heading"><div class="case-page-kicker"><span>PAGE ${String(pageIndex+1).padStart(2,"0")}</span><span>${completion.isComplete?`${navIcon("check",15)} 完了`:completion.required?`${completion.completed}/${completion.required}項目`:"READ"}</span></div><h1 class="page-title">${escapeHtml(page.title)}</h1>${page.intro?`<p class="page-lead">${escapeHtml(page.intro)}</p>`:""}</header>${renderPageBody(data,page)}${renderCaseFooter(data,pageIndex)}</main>`;
}

function renderFinancialStatement(doc) {
  const rows=doc.rows||doc.sections?.flatMap((section)=>[{sectionLabel:section.label},...section.rows])||[];
  const mobileRows=rows.map((row)=>row.sectionLabel?`<h4 class="mobile-section-label">${escapeHtml(row.sectionLabel)}</h4>`:`<article class="financial-mobile-row"><strong>${escapeHtml(row.label)}</strong><div>${row.values.map((value,index)=>`<button class="mobile-value" data-action="copy-number" data-value="${value}"><small>${escapeHtml(doc.periods[index])}</small><b>${formatNumber(value)}</b><em>${escapeHtml(doc.unit)}</em></button>`).join("")}</div></article>`).join("");
  return `<section class="card document-card"><header><div><span class="document-type">FINANCIAL STATEMENT</span><h3>${escapeHtml(doc.title)}</h3></div><small>単位：${escapeHtml(doc.unit)}</small></header><div class="desktop-financial-table table-scroll"><table><thead><tr><th>科目</th>${doc.periods.map((period)=>`<th>${escapeHtml(period)}</th>`).join("")}</tr></thead><tbody>${rows.map((row)=>row.sectionLabel?`<tr class="section-row"><td colspan="${doc.periods.length+1}">${escapeHtml(row.sectionLabel)}</td></tr>`:`<tr><td>${escapeHtml(row.label)}</td>${row.values.map((value,index)=>`<td><button class="value-button" data-action="copy-number" data-value="${value}" aria-label="${escapeHtml(row.label)} ${doc.periods[index]} ${value}${doc.unit}">${formatNumber(value)}</button></td>`).join("")}</tr>`).join("")}</tbody></table></div><div class="mobile-financial-list">${mobileRows}</div></section>`;
}

function renderGenericTable(doc) {
  return `<section class="card document-card"><header><div><span class="document-type">DETAIL TABLE</span><h3>${escapeHtml(doc.title)}</h3></div><small>単位：${escapeHtml(doc.unit||"-")}</small></header><div class="desktop-financial-table table-scroll"><table><thead><tr>${doc.columns.map((column)=>`<th>${escapeHtml(column.label)}</th>`).join("")}</tr></thead><tbody>${doc.rows.map((row)=>`<tr>${doc.columns.map((column)=>`<td>${typeof row[column.id]==="number"?`<button class="value-button" data-action="copy-number" data-value="${row[column.id]}">${formatNumber(row[column.id])}</button>`:escapeHtml(String(row[column.id]??""))}</td>`).join("")}</tr>`).join("")}</tbody></table></div><div class="mobile-generic-list">${doc.rows.map((row)=>`<article>${doc.columns.map((column)=>`<div><small>${escapeHtml(column.label)}</small>${typeof row[column.id]==="number"?`<button data-action="copy-number" data-value="${row[column.id]}">${formatNumber(row[column.id])}<em>${escapeHtml(doc.unit||"")}</em></button>`:`<strong>${escapeHtml(String(row[column.id]??""))}</strong>`}</div>`).join("")}</article>`).join("")}</div></section>`;
}

